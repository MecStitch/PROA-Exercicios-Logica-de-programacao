let contadora, resultado, numero;

for (contadora = 1; contadora <= 15; contadora++); {
resultado = Math.pow(contadora,3);
 alert(contadora + "elevado a 3 é igual a:" + resultado);
}
document.getElementById("resposta").innerHTML = contadora + " elevado a 3 é igual a: " + numero;
